# Example environment file used with docker-compose
# Copy this to .env.compose and customise if needed, then run:
# docker compose --env-file .env.compose up --build

# MySQL
DB_HOST=db
DB_USER=root
DB_PASS=Giltech@2025
DB_NAME=giltechdb

# App
PORT=5000
JWT_SECRET=change_this_dev_secret
ADMIN_TOKEN=testtoken
